name: Build Tweak

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (force continue even if errors)
        run: |
          sudo apt update || true
          sudo apt install -y fakeroot git perl build-essential unzip curl wget rsync libxml2-utils libncurses6 || true

          # 强制下载 libtinfo5 deb (清华镜像 + 官方 fallback)
          wget https://mirrors.tuna.tsinghua.edu.cn/ubuntu/pool/main/n/ncurses/libtinfo5_6.3-2_amd64.deb -O libtinfo5.deb || true
          sudo dpkg -i libtinfo5.deb || sudo apt install -f -y || true

          rm *.deb || true

      - name: Setup Theos
        uses: Randomblock1/theos-action@v1
        with:
          theos-dir: theos
          theos-src: https://github.com/theos/theos
          theos-sdks: https://github.com/xybp888/iOS-SDKs
          theos-sdks-branch: master
          orion: false

      - name: Cache Theos & SDKs
        uses: actions/cache@v4
        with:
          path: |
            theos
            .theos
          key: ${{ runner.os }}-theos-${{ hashFiles('Makefile', 'control', 'Tweak.x') }}

      - name: Build & Package
        run: |
          make clean || true
          make package FINALPACKAGE=1 || true
        env:
          THEOS_PACKAGE_SCHEME: rootless
          THEOS: ${{ github.workspace }}/theos
          ARCHS: arm64 arm64e

      - name: Upload artifacts (always upload if any files exist)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: plugin-build
          path: |
            packages/*.deb
            .theos/obj/*.dylib
            .theos/obj/**/*
          if-no-files-found: warn
